<!doctype html>
<html lang="pt-BR">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Manual Visual — Onboarding de Compra Aprovada no UnniChat</title>
<style>
  :root{
    --bg:#f6f3ee; --paper:#ffffff; --ink:#17202a; --muted:#667085; --line:#e6e0d8;
    --navy:#123047; --wine:#6f2436; --gold:#b78b3b; --teal:#1f666d; --green:#197447;
    --red:#b42318; --amber:#b54708; --soft-navy:#edf3f7; --soft-wine:#f8eef1;
    --soft-gold:#fbf5e8; --soft-green:#ecf8f1; --soft-red:#fff0ee; --shadow:0 14px 38px rgba(18,48,71,.10);
    --radius:18px;
  }
  *{box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",Roboto,Arial,sans-serif;line-height:1.58}
  a{color:var(--teal)}
  .shell{display:grid;grid-template-columns:290px minmax(0,1fr);min-height:100vh}
  aside{position:sticky;top:0;height:100vh;background:var(--navy);color:#fff;padding:24px 18px;overflow:auto}
  .brand{font-size:13px;letter-spacing:.12em;text-transform:uppercase;opacity:.72;margin-bottom:10px}
  aside h1{font-size:21px;line-height:1.18;margin:0 0 14px}
  .date{font-size:12px;opacity:.72;margin-bottom:20px}
  .progress-wrap{background:rgba(255,255,255,.12);height:7px;border-radius:99px;overflow:hidden;margin:8px 0 8px}
  .progress{height:100%;width:0;background:#e6c779;transition:.25s}
  .progress-label{font-size:12px;opacity:.78;margin-bottom:18px}
  nav a{display:block;color:#eaf0f4;text-decoration:none;padding:8px 10px;border-radius:10px;font-size:13px;margin:2px 0}
  nav a:hover{background:rgba(255,255,255,.10)}
  aside .mini{font-size:12px;opacity:.75;margin-top:22px}
  main{max-width:1180px;padding:40px 44px 100px;width:100%}
  .hero{background:linear-gradient(135deg,var(--navy),#1e4d65 62%,#245c62);color:#fff;border-radius:26px;padding:42px;box-shadow:var(--shadow);position:relative;overflow:hidden}
  .hero:after{content:"";position:absolute;right:-90px;top:-120px;width:320px;height:320px;border:1px solid rgba(255,255,255,.16);border-radius:50%;box-shadow:0 0 0 50px rgba(255,255,255,.04),0 0 0 100px rgba(255,255,255,.025)}
  .eyebrow{font-size:12px;letter-spacing:.14em;text-transform:uppercase;font-weight:800;color:#eed99b}
  .hero h2{font-size:42px;line-height:1.05;margin:10px 0 14px;max-width:820px}
  .hero p{font-size:17px;max-width:870px;color:#e9f0f4;margin:0}
  .hero-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:28px;position:relative;z-index:2}
  .hero-stat{border:1px solid rgba(255,255,255,.16);border-radius:14px;padding:14px;background:rgba(255,255,255,.06)}
  .hero-stat b{display:block;font-size:18px;color:#fff}
  .hero-stat span{font-size:12px;color:#d8e4ea}
  section{scroll-margin-top:20px;margin-top:38px}
  h2.section-title{font-size:29px;line-height:1.15;color:var(--navy);margin:0 0 10px}
  .lead{font-size:16px;color:#475467;max-width:940px;margin:0 0 20px}
  .card{background:var(--paper);border:1px solid var(--line);border-radius:var(--radius);padding:24px;box-shadow:0 7px 22px rgba(30,42,54,.045);margin:14px 0}
  .card h3{margin:0 0 8px;font-size:19px;color:var(--navy)}
  .card h4{margin:20px 0 7px;color:var(--wine);font-size:15px}
  .grid-2{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}
  .grid-3{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}
  .pill{display:inline-block;border-radius:999px;padding:4px 9px;font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}
  .pill.blue{background:var(--soft-navy);color:var(--navy)} .pill.wine{background:var(--soft-wine);color:var(--wine)}
  .pill.gold{background:var(--soft-gold);color:#725313} .pill.green{background:var(--soft-green);color:var(--green)}
  .callout{border-left:5px solid var(--gold);background:var(--soft-gold);padding:17px 18px;border-radius:12px;margin:16px 0}
  .callout.info{border-color:var(--teal);background:#edf7f8}.callout.warn{border-color:var(--amber);background:#fff4e8}.callout.danger{border-color:var(--red);background:var(--soft-red)}.callout.ok{border-color:var(--green);background:var(--soft-green)}
  .callout b{color:var(--navy)}
  .flow{display:flex;align-items:stretch;gap:8px;flex-wrap:wrap;margin:18px 0 4px}
  .node{flex:1 1 145px;min-width:130px;background:#fff;border:1px solid var(--line);border-radius:14px;padding:13px 12px;position:relative}
  .node .n{font-size:10px;font-weight:900;color:var(--muted);text-transform:uppercase;letter-spacing:.08em}
  .node b{display:block;font-size:13px;color:var(--navy);margin-top:3px}.node span{font-size:11px;color:var(--muted)}
  .arrow{align-self:center;color:var(--gold);font-size:22px;font-weight:900}
  .phase{border-top:7px solid var(--navy)} .phase.wine{border-top-color:var(--wine)} .phase.teal{border-top-color:var(--teal)} .phase.gold{border-top-color:var(--gold)}
  .step{display:grid;grid-template-columns:58px 1fr;gap:16px;margin:20px 0}
  .step-num{width:48px;height:48px;border-radius:14px;background:var(--navy);color:#fff;display:flex;align-items:center;justify-content:center;font-weight:900;font-size:18px;box-shadow:0 8px 18px rgba(18,48,71,.14)}
  .step-body{background:#fff;border:1px solid var(--line);border-radius:16px;padding:18px 20px}
  .step-body h3{margin:0 0 5px;color:var(--navy);font-size:18px}.step-body p{margin:6px 0}
  ol,ul{padding-left:22px} li{margin:6px 0}
  code,.code{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;background:#f3f4f6;border:1px solid #e4e7ec;border-radius:7px;padding:1px 5px;font-size:.92em}
  pre{white-space:pre-wrap;background:#111b23;color:#edf2f5;padding:16px;border-radius:13px;overflow:auto;font-size:12px;line-height:1.5}
  .copybox{position:relative;background:#111b23;color:#edf2f5;padding:16px 95px 16px 16px;border-radius:13px;white-space:pre-wrap;font-family:ui-monospace,Menlo,monospace;font-size:12px;margin:10px 0}
  .copybtn{position:absolute;right:10px;top:10px;border:0;border-radius:8px;padding:7px 10px;cursor:pointer;background:#e7c879;color:#222;font-weight:800;font-size:11px}
  .check{display:flex;gap:11px;align-items:flex-start;margin:10px 0}.check input{margin-top:5px;transform:scale(1.2)}.check label{cursor:pointer}
  .check.done label{text-decoration:line-through;color:#98a2b3}
  .kpi{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}.kpi div{border:1px solid var(--line);border-radius:14px;padding:14px;background:#fff}.kpi b{display:bloc